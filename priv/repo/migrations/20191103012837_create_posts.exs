defmodule TimelogApp.Repo.Migrations.CreatePosts do
  use Ecto.Migration

  def change do
    # create table(:posts) do
    #   add :name, :string
    #   add :desc, :text
    #   add :approved, :boolean, default: false, null: false

    #   timestamps()
    # end

  end
end
