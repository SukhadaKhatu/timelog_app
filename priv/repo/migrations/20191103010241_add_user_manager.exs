defmodule TimelogApp.Repo.Migrations.AddUserManager do
  use Ecto.Migration

  def change do
    # alter table(:requests) do
    #   add :user, references(:users)
    #   add :handles, references(:users)
    # end
  end
end
