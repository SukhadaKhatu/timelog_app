defmodule TimelogApp.Repo.Migrations.AddManager do
  use Ecto.Migration

  def change do
    # alter table(:Managers) do
    #   add :manager, :boolean, default: false, null: false
    # end
  end
end
